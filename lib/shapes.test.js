const {Triangle, Square, Circle} = require('./lib/shapes.js');

Test('Triangle renders correcly', () => {
    
});

test('Circle renders correcly', () => {

});

test('Square renders correcly', () => {

});